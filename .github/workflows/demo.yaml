name: Demo pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main

run-name: Demo ${{ github.event.head_commit.message }}

permissions:
  id-token: write
  contents: read
  
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
    - run: echo ${{ github.event_name }} ${{ github.event }}  ${{ github.ref_name }}

           
